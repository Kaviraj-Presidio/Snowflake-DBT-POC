version: 2

models:
  - name: silver_customers
    description: Cleaned customer records; rows without an email are filtered out.
    columns:
      - name: customer_id
        data_tests:
          - unique
          - not_null
      - name: email
        data_tests:
          - not_null
